/** Fixed Feature: Model inference (api05) **/
export async function routeInference(payload) { const { model, messages } = payload; console.log(`[API] Routing inference to ${model}`); const res = await fetch('http://127.0.0.1:3001/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ messages }) }); return await res.json(); }