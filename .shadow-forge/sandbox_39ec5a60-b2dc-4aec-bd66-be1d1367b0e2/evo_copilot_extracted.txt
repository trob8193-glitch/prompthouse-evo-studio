Created At: 2026-06-19T00:57:15Z
Completed At: 2026-06-19T00:57:15Z

				The command completed successfully.
				Output:
				commit 9e8e8fe4e4f074de6302141a016dd4926765b688
Author: trob8193-glitch <trob8193@gmail.com>
Date:   Sat Jun 13 17:46:12 2026 -0500

    feat: holographic module remaster + bridge syntax fix + framer-motion stagger animations + evo training pipeline seeded
    
    - Refactored ScreenTemplate with framer-motion stagger children animations
    - Introduced .module-container and .evo-grid CSS architecture
    - Upgraded PerformanceMonitor, AutonomousBuilderView, BotStageView, MasterPromptVaultView to holographic module system
    - Fixed escaped backtick syntax errors in UniversalAIAdaptor.js (bridge crash fix)
    - Seeded studio-internal-module-training.json for Evo LLM pipeline
    - Updated IDEPageLayout with living ambient background
    - Modernized Tailwind v4 syntax across index.jsx
    - Global glassmorphic scrollbar, button, and card token upgrades in index.css

diff --git a/src/components/EvoCopilot.jsx b/src/components/EvoCopilot.jsx
index 4a873bbb..f9497459 100644
--- a/src/components/EvoCopilot.jsx
+++ b/src/components/EvoCopilot.jsx
@@ -38,6 +38,7 @@ export function EvoCopilot() {
   const fileContent = useSovereignStore((s) => s.fileContent);
   const addTerminalLogs = useSovereignStore((s) => s.addTerminalLogs);
   const setActivePage = useSovereignStore((s) => s.setActivePage);
+  const bondedNodes = useSovereignStore((s) => s.bondedNodes);
 
   const currentBot = ALL_BOT_ROSTER.find(b => b.id === selectedBot) || ALL_BOT_ROSTER[0];
 
@@ -165,10 +166,12 @@ export function EvoCopilot() {
 
       const botSystemPrompt = `You are ${currentBot.name}, a ${currentBot.species}. Role: ${currentBot.role}. Signature: ${currentBot.signature}.\nOutput clean markdown. Use \`\`\`language blocks for code.`;
 
+      const isBonded = bondedNodes && bondedNodes.length > 0;
+
       // Route all bots through universalSend to guarantee QuadBrain fallbacks
       const res = await universalSend(payloadMessages, botSystemPrompt, {
         model: targetBotId === 'evo' ? 'evo-llm-swarm' : 'gpt-4.1-mini',
-        provider: 'evo'
+        provider: isBonded ? 'bonded_ide' : 'evo'
       });
 
       assistantContent = res.message || "No response generated.";

