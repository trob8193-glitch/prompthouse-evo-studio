import { HonestFallbackClass } from '../fallback-marker.js';

/**
 * ModelRegistry — Registry of available AI models with capability metadata
 * Status: NOT_IMPLEMENTED (honest fallback)
 */
export class ModelRegistry extends HonestFallbackClass {
  constructor() { super('ModelRegistry', 'Registry of available AI models with capability metadata'); }
}
