import { HonestFallbackClass } from '../fallback-marker.js';

/**
 * RealityTwinRoutes — API routes for digital twin state management
 * Status: NOT_IMPLEMENTED (honest fallback)
 */
export class RealityTwinRoutes extends HonestFallbackClass {
  constructor() { super('RealityTwinRoutes', 'API routes for digital twin state management'); }
}
